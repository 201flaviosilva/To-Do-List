import Counter from "../../components/Counter";

export default function Home() {
	return (
		<div className="App">
			<h1>Vite + TypeScript + React + Redux + React Router Dom</h1>
			<Counter />
		</div>
	);
}
