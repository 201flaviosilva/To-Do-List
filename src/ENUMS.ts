/* eslint-disable no-unused-vars */ // This is not right configured
export enum PAGES {
  HOME = "/",
  ACCOUNT = "/account",
  NOT_FOUNT = "*",
}

export enum LOCAL_STORAGE {
  USERS = "USERS",
  CURRENT_USER = "CURRENT_USER",
  TASKS = "TASKS",
}

export enum DEFAULT_USERS_IDS {
  ADMIN = "A",
  GUEST = "G",
}
